# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
node_modules.nosync
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python bytecode from scratch/ generator scripts
__pycache__/
*.pyc

# Half-written content files from the DevOps authoring pipeline: the second stage
# merges each _partial_ into its final <subject>.json and deletes it, so one only
# exists mid-run and must never be committed.
src/data/devops/_partial_*.json
